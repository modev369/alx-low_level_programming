#ifndef MAIN_H
#define MAIN_H

/* Included libraries */
#include <unistd.h>

/* Function prototypes */
void _puts_recursion(char *s);
int _putchar(char c);
void _print_rev_recursion(char *s);
int _strlen_recursion(char *s);
#endif /* MAIN_H */
